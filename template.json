{
  "dnsname": "{{ clusterid }}",
  "nodes": {
    "postgresqlbeta_node0": {
      "docker_image": "docker-registry.cesga.es:5000/postgresql:9.3",
      "docker_opts": "-e POSTGRES_PASSWORD={{ opts['password'] }} -e POSTGRES_USER={{ opts['user'] }} ",
      "cpu": {{ opts['cpu'] }},
      "mem": {{ opts['mem'] }},
      "disks": {
        "disk0": {
          "destination":  "/data"
        }
      },
      "networks": {
        "eth0": {"type": "dynamic", "networkname": "storage"},
        "eth1": {"type": "dynamic", "networkname": "admin" }
      }
    }
  },
  "services": {
      "postgresqlbeta": { "nodes": ["postgresqlbeta_node0"] }
  }
}
